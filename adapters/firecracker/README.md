# Adaptador Firecracker

Solo para Linux con KVM. Requiere kernel, rootfs, configuración de red y un
proceso jailer. `v0.6.0` no descarga imágenes ni inicia microVMs automáticamente.
Mantén las imágenes fuera de Git y registra sus hashes en la evidencia.
