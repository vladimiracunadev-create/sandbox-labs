# Bubblewrap

Estado experimental. El adaptador prepara namespaces, sesión independiente, entorno limpio, capabilities vacías, workload read-only y output efímero.

Pendientes para `ready`: cgroups v2, seccomp efectivo, validación multi-distro y pruebas negativas ejecutadas en CI/VM.
