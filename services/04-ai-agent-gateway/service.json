{
  "$schema": "../../schemas/service.schema.json",
  "id": "ai-agent-gateway",
  "name": "Agente de IA con egress bajo allowlist",
  "category": "platform",
  "description": "Un agente que llama a la API de un modelo desde dentro del sandbox, con salida de red restringida a los hosts que la política declara.",
  "teaches": "Que un proceso puede necesitar internet y aun así no tener internet: solo los destinos declarados, y el resto queda registrado como intento denegado.",
  "port": 8804,
  "kind": "python",
  "entrypoint": "app.py",
  "command": "python3",
  "args": [],
  "policy": "ai-agent-restricted",
  "runtimes": [
    "bwrap",
    "unshare"
  ],
  "healthPath": "/health",
  "secrets": [
    "SANDBOX_AI_API_KEY",
    "SANDBOX_AI_ENDPOINT",
    "SANDBOX_EGRESS_ALLOWLIST"
  ]
}
