# Memory pressure

Carga educativa para comprobar que el runtime corta una ejecución al alcanzar el límite de memoria.

La implementación real se deja deshabilitada en `v0.6.0`. Antes de agregarla, valida el laboratorio en una VM o microVM y evita ejecutarla directamente en el host.
