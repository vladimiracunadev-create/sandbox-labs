# Benchmarks

Usa la misma máquina, workload, policy y versión de runtime. Ejecuta al menos 10 repeticiones, descarta warm-up de forma explícita y publica mediana, p95 y dispersión.

No mezcles resultados de hosts distintos. Conserva la evidencia individual y agrega solo mediciones reales a `benchmarks/matrix.json`.
